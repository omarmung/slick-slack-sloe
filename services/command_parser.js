// parse incoming commands from slack /command POST request body text
export function parseCommand(bodyText) {
  
  switch(bodyText) {
    case n:
      // code block
      break;
    case n:
      // code block
      break;
    default:
      // code block
}
}