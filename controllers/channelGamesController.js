const parseCommand = require('../services/command_parser.js')

// handle requests for channel/games resources from models
export function gameCommandHandler(req, res) {
  switch(expression) {
    case n:
      // code block
      break;
    case n:
      // code block
      break;
    default:
      // code block
}
}